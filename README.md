# Candor Post Website

### Requirements
- A good IDE
- Java 11
- Gradle (optional)
- Curl (optional, but helps debugging)

### Updating submodules
- Run `git submodule foreach git pull origin main`
- Commit and push
